# Mensaje para pegarle a Claude

Copiá y pegá esto en Claude Code (abierto dentro de esta carpeta), adjuntando la captura de la
sección que quieras maquetar:

---

Estoy maquetando en HTML una sección del formulario de alta del software "el Salar", como parte del
proyecto Souly HR. **Antes de hacer nada, leé el archivo `CLAUDE.md` de esta carpeta** — ahí está
todo el contexto, las reglas y el patrón HTML que tenés que seguir.

Te adjunto la **captura de la sección "[NOMBRE DE LA SECCIÓN]"**. Necesito que:

1. Identifiques cada campo de la captura: etiqueta, tipo (texto/desplegable/fecha/número/radio/
   checkbox), si es obligatorio (el Salar los marca con `(*)` en rojo) y las opciones de los
   desplegables si se ven.
2. Generes el archivo `secciones/[NN-nombre].html` siguiendo EXACTAMENTE el patrón del CLAUDE.md
   y del ejemplo `secciones/00-reingreso-colaborador_EJEMPLO.html`.
3. Uses **solo** las clases del CSS compartido. **No modifiques `assets/styles.css` ni `assets/accordion.js`.**
4. Maquetes **solo lo que se ve en la captura** — no inventes campos. Si algo no se lee, dejá un
   comentario `<!-- revisar -->` y avisame.

Mis secciones asignadas son: **Cónyuge, Contacto de emergencia, Detalle de garante, Detalle de hijos.**

Cuando termines, decime cómo abrir el archivo para revisarlo contra la captura.

---
